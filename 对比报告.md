# 接口对比报告 — 现状代码 vs 原接口文档

> 对比对象：
> - 现状：`lb-children` 重构后代码（Controller / DTO / 统一返回）
> - 原始：飞书《接口文档》`https://ocnxm7f4j12f.feishu.cn/wiki/UnQOwnQ1tiaVq9k7K4dcSC2jnqd`

## 1. 总体结论

原文档定义的 **20 个接口全部保留且路径/方法/参数完全兼容**，未删除或改动任何既有端点。差异集中在三处：

1. **响应格式全局统一**（裸对象 / `{message}` / `{error}` → `Result{code,msg,data}`）；
2. **上传相册由"传图片地址"升级为"真实文件上传"**；
3. **新增了资源学习进度、聊天亲子校验等能力**（原文档未包含）。

## 2. 逐接口对比

### 2.1 完全一致（路径 / 方法 / 入参）

| 接口 | 方法 | 原文档 | 现状代码 | 说明 |
|---|---|---|---|---|
| `/api/parent/register` | POST | ✓ | ✓ | 一致 |
| `/api/parent/login` | POST | ✓ | ✓ | 一致 |
| `/api/parent/add-child` | POST | ✓ | ✓ | 一致（PARENT） |
| `/api/parent/my-children` | GET | ✓ | ✓ | 一致（PARENT） |
| `/api/child/login` | POST | ✓ | ✓ | 一致 |
| `/api/child/add-diary` | POST | ✓ | ✓ | 一致（CHILD） |
| `/api/child/my-diaries` | GET | ✓ | ✓ | 一致（CHILD） |
| `/api/child/my-albums` | GET | ✓ | ✓ | 一致（CHILD） |
| `/api/resources` | GET | ✓ | ✓ | 一致（公开） |
| `/api/resources/types` | GET | ✓ | ✓ | 一致（公开） |
| `/api/ai/ask` | POST | ✓ | ✓ | 一致 |
| `/api/ai/history` | GET | ✓ | ✓ | 一致 |
| `/api/chat/history?otherUserId=` | GET | ✓ | ✓ | 一致 |
| `/api/treehole/posts` | POST / GET | ✓ | ✓ | 一致 |
| `/api/treehole/posts/{postId}` | GET | ✓ | ✓ | 一致 |
| `/api/treehole/posts/{postId}/replies` | POST / GET | ✓ | ✓ | 一致 |
| WebSocket `/ws?token=` | STOMP | ✓ | ✓ | 一致（`/app/chat`、`/user/{id}/queue/messages`） |

### 2.2 有变更的接口

| 接口 | 原文档 | 现状代码 | 差异说明 |
|---|---|---|---|
| `/api/child/upload-album` | 参数 `imageUrl`（form 表单传图片地址），返回 `{message:"相册上传成功"}` | 参数 `file`（`multipart/form-data` 真实文件上传）+ 可选 `description`，返回图片访问 URL | **按需求将"存地址"升级为"真上传"**，新增 `FileStorageService` 落盘 `/uploads/**` 静态访问。前端需改用 `FormData` 上传文件 |

### 2.3 新增接口（原文档未定义）

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/resources/{resourceId}/progress` | POST | 更新学习进度（`userId` 取自 JWT，防越权） |
| `/api/resources/{resourceId}/progress` | GET | 查询单资源进度 |
| `/api/resources/progress` | GET | 查询我的全部进度 |
| `/api/resources/{resourceId}/progress` | DELETE | 删除进度 |

## 3. 响应格式对比（全局性差异）

| 场景 | 原文档 | 现状代码 |
|---|---|---|
| 成功（返回数据） | 直接返回数据对象/数组 | `{code:200, msg:"success", data:{...}}` |
| 成功（无数据） | `{"message":"注册成功"}` | `{code:200, msg:"success", data:"注册成功"}` |
| 业务失败 | HTTP `400/401` + `{"error":"具体错误信息"}` | HTTP 200 + `{code:100x, msg:"具体错误信息", data:null}` |
| 资源类型 | `{"types":["COURSE","VIDEO"]}` | `{code:200, data:["COURSE","VIDEO"]}`（结构统一，data 内为数组） |

> 结论：**响应载体不同，但字段语义一一对应**。前端如已按原文档联调，需将取值从 `data` 字段改为 `res.data.data`（或统一用 `res.data` 解包）。

## 4. 错误码体系对比

| 原文档 | 现状代码 |
|---|---|
| 文本错误 + HTTP 400/401 | 结构化错误码（`ResultCode` 枚举） |
| 无统一错误码表 | `200/400/401/403/404/1001/1002/1003/1004/1005/1006/1007/500/5001/5002` |

原文档列出的常见错误均有对应：

- `用户名已存在` → 1001
- `用户名或密码错误` → 1002
- `未找到该儿童，请检查昵称` → 1004
- `您的问题包含不适当内容` → 1006（敏感词过滤）
- `今日提问次数已达上限` → 1005
- 新增：`只能与自己关联的家长或儿童聊天` → 1007（聊天亲子校验，原文档未含此安全约束）

## 5. 新增能力（原文档未涉及）

1. **聊天亲子关系校验**：家长只能给孩子发消息、儿童只能给家长发消息（防陌生人骚扰）。
2. **敏感词过滤 + 文件化词库**：AI 提问前过滤，词库从 `sensitive-words.txt` 加载（原文档仅有提示文案）。
3. **学习进度防越权**：`userId` 一律取自 JWT，不接受客户端传入。
4. **敏感配置拆分**：DB 密码 / JWT 密钥 / AI Key 移入 `application-local.yaml`（不入库、不入 Git）。

## 6. 兼容性与风险提示

- **路径兼容**：100%，前端无需改 URL。
- **响应解包**：需按 `Result` 结构调整（唯一前端改动点）。
- **upload-album**：必须改为 `FormData` + `file` 字段上传。
- **认证方式**：`Authorization: Bearer <token>` 完全一致。
- **WS**：端点 `/ws?token=` 与订阅主题一致，无需改动。

## 7. 建议

1. 前端联调前先同步《接口文档》（本项目版本），重点看统一返回与 `upload-album` 变更。
2. 编译验证通过后跑一遍冒烟（注册→登录→加儿童→儿童登录→日记/相册/AI/聊天/树洞）。
