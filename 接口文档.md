# 留守儿童关爱平台后端 — 接口文档

## 1. 通用约定

- **Base URL**：`http://localhost:8080`（可配置）
- **鉴权**：除登录/注册外均需携带请求头 `Authorization: Bearer <token>`；角色 `PARENT` / `CHILD`
- **响应格式**（统一 `Result<T>`）：

```json
{ "code": 200, "msg": "success", "data": { } }
```

- **错误码**：

| code | 含义 |
|---|---|
| 200 | 成功 |
| 400 | 参数错误 |
| 401 | 未登录或 Token 无效 |
| 403 | 权限不足 |
| 404 | 资源不存在 |
| 1001 | 用户名已存在 |
| 1002 | 用户名或密码错误 |
| 1003 | 家长账号不存在 |
| 1004 | 儿童不存在 |
| 1005 | 今日提问次数已达上限 |
| 1006 | 内容包含不适当信息 |
| 1007 | 只能与自己关联的家长或儿童聊天 |
| 5001 | 数据库操作失败 |
| 5002 | AI 服务暂时不可用 |
| 500 | 服务器内部错误 |

- 校验失败 / 业务异常由全局异常处理器统一返回上述错误码，接口正常返回 200 + code。

---

## 2. 家长端 `/api/parent`

### 2.1 注册 `POST /api/parent/register`

请求：

```json
{ "username": "parent1", "password": "123456", "phone": "13800138000" }
```

响应：`Result<String>`（"注册成功"）

### 2.2 登录 `POST /api/parent/login`

请求：

```json
{ "username": "parent1", "password": "123456" }
```

响应：`Result<{ token: string, role: "PARENT" }>`

### 2.3 添加儿童 `POST /api/parent/add-child`（PARENT）

请求：

```json
{ "nickname": "小明", "age": 10 }
```

响应：`Result<String>`（"儿童添加成功"）

### 2.4 我的儿童 `GET /api/parent/my-children`（PARENT）

响应：`Result<List<ChildVO>>`，`ChildVO = { id, nickname, age, avatar }`

---

## 3. 儿童端 `/api/child`

### 3.1 登录 `POST /api/child/login`

请求：

```json
{ "parentUsername": "parent1", "parentPassword": "123456", "childNickname": "小明" }
```

响应：`Result<{ token: string, role: "CHILD" }>`

### 3.2 写日记 `POST /api/child/add-diary`（CHILD）

请求：

```json
{ "content": "今天很开心", "imageUrl": "http://...", "isAnonymous": false }
```

响应：`Result<String>`（"日记保存成功"）

### 3.3 我的日记 `GET /api/child/my-diaries`（CHILD）

响应：`Result<List<Diary>>`，`Diary = { id, content, imageUrl, isAnonymous, childId, createdAt }`

### 3.4 上传相册 `POST /api/child/upload-album`（CHILD，`multipart/form-data`）

参数：

- `file`（必填）：图片文件，支持 jpg/jpeg/png/gif/webp，≤ 10MB
- `description`（可选）：图片描述

响应：`Result<String>`（图片访问 URL，如 `http://localhost:8080/uploads/album/xxx.jpg`）

### 3.5 我的相册 `GET /api/child/my-albums`（CHILD）

响应：`Result<List<Album>>`，`Album = { id, imageUrl, description, childId, createdAt }`

---

## 4. 教育资源 `/api/resources`

### 4.1 资源列表 `GET /api/resources`（公开）

参数：`type`（可选，如 VIDEO / COURSE / ARTICLE）

响应：`Result<List<EduResource>>`，`EduResource = { id, title, type, url, description, coverUrl }`

### 4.2 资源类型 `GET /api/resources/types`（公开）

响应：`Result<List<String>>`

### 4.3 更新学习进度 `POST /api/resources/{resourceId}/progress`（登录）

请求：

```json
{ "progressPercent": 50 }
```

响应：`Result<LearningProgress>`，`LearningProgress = { id, userId, resourceId, progressPercent, lastLearnTime, createTime, updateTime }`

> `userId` 取自 JWT，防止越权。

### 4.4 查询单资源进度 `GET /api/resources/{resourceId}/progress`（登录）

响应：`Result<LearningProgress>`；无记录时 `Result<{ message: "No progress found" }>`

### 4.5 查询我的全部进度 `GET /api/resources/progress`（登录）

响应：`Result<List<LearningProgress>>`

### 4.6 删除进度 `DELETE /api/resources/{resourceId}/progress`（登录）

响应：`Result<String>`（"进度删除成功"）

---

## 5. 树洞倾诉 `/api/treehole`

### 5.1 发帖 `POST /api/treehole/posts`（登录）

请求：

```json
{ "content": "想爸爸了", "imageUrl": "http://..." }
```

响应：`Result<TreeholePostResponse>`，`TreeholePostResponse = { id, content, imageUrl, createdAt, replyCount, replies? }`

### 5.2 帖子列表 `GET /api/treehole/posts`（登录）

响应：`Result<List<TreeholePostResponse>>`（不含 replies）

### 5.3 帖子详情 `GET /api/treehole/posts/{postId}`（登录）

响应：`Result<TreeholePostResponse>`（含 replies 列表）

### 5.4 回复帖子 `POST /api/treehole/posts/{postId}/replies`（登录）

请求：

```json
{ "content": "加油，我们都在", "imageUrl": "http://..." }
```

响应：`Result<TreeholeReplyResponse>`，`TreeholeReplyResponse = { id, content, imageUrl, createdAt }`

### 5.5 帖子回复列表 `GET /api/treehole/posts/{postId}/replies`（登录）

响应：`Result<List<TreeholeReplyResponse>>`

---

## 6. AI 助手 `/api/ai`

### 6.1 提问 `POST /api/ai/ask`（登录）

请求：

```json
{ "question": "想妈妈了怎么办？" }
```

前置校验：敏感词过滤（`CONTENT_SENSITIVE`）、儿童每日次数限制（`AI_LIMIT_EXCEEDED`）

响应：`Result<AIChatResponse>`，`AIChatResponse = { answer, historyId }`

### 6.2 提问历史 `GET /api/ai/history`（登录）

响应：`Result<List<AIChatHistory>>`，`AIChatHistory = { id, userId, userRole, question, answer, createdAt }`

---

## 7. 实时聊天（WebSocket + HTTP）

### 7.1 WebSocket 连接

- 端点：`ws://localhost:8080/ws`（STOMP），握手需带 `token` 参数（JWT）
- 发送：`/app/chat`，消息体：

```json
{
  "receiverId": 1,
  "content": "你好",
  "messageType": "CHAT",
  "senderType": "PARENT"
}
```

- 订阅接收：`/user/{receiverId}/queue/messages`
- **亲子校验**：家长只能发给自己的孩子，儿童只能发给自己的家长，否则推送拒绝（错误码 1007）

### 7.2 聊天历史 `GET /api/chat/history?otherUserId={id}`（登录）

响应：`Result<List<Message>>`，`Message = { id, senderType, senderId, receiverId, content, mediaUrl, messageType, createdAt }`
