# 留守儿童关爱平台后端 — 项目总结

## 1. 项目概述

面向留守儿童及其外出务工家长的关爱服务平台后端，提供家长端、儿童端、实时聊天、AI 心理支持、教育资源学习、树洞倾诉等核心能力。

本次工作完成了**架构级重构**（对齐 `demo1/设计4.md` 的标准程度），并补齐了若干功能需求。

## 2. 技术栈

| 分类 | 技术 |
|---|---|
| 语言 / 框架 | Java 21、Spring Boot 3.2.5 |
| ORM | MyBatis-Plus 3.5.7（原 Spring Data JPA 已移除） |
| 数据库 | MySQL 8.x（原 H2 内存库已移除） |
| 安全 | Spring Security + JWT（jjwt 0.12.3） |
| 实时通信 | Spring WebSocket（STOMP） |
| 校验 | spring-boot-starter-validation |
| 构建 | Maven（mvnw） |

## 3. 目录结构与分层

```
src/main/java/com/platform/lbchildren
├── common      # 统一返回 Result、错误码 ResultCode、业务异常、全局异常处理
├── config      # Security / MyBatis-Plus / WebSocket / CORS / RestTemplate / 静态资源 / 数据初始化
├── controller  # 6 个控制器（接口层，只依赖 Service 接口）
├── dto         # 请求/响应 DTO（含 JSR-303 校验注解）
├── entity      # 10 个 MyBatis-Plus 实体（@TableName，去掉 JPA 关系映射）
├── mapper      # 10 个 BaseMapper 接口
├── security    # JWT 工具、认证过滤器、WebSocket 认证、UserPrincipal
└── service
    ├── 接口（ParentService / ChildService / ResourceService / ChatService / AIChatService / TreeholeService / FileStorageService）
    └── impl  # 对应实现类
```

## 4. 核心模块

| 模块 | 说明 |
|---|---|
| 家长端 | 注册、登录、添加儿童、查看我的儿童 |
| 儿童端 | 登录（家长账号 + 儿童昵称）、写日记、相册上传（真实文件上传）、查看日记/相册 |
| 实时聊天 | WebSocket 收发 + 历史记录查询；新增**亲子关系校验**，只能与自己的家人聊天 |
| AI 助手 | DeepSeek 大模型，含内容安全过滤（敏感词库）、儿童每日次数限制、历史记录 |
| 教育资源 | 资源列表/类型、学习进度记录（含防越权：userId 一律取自 JWT） |
| 树洞倾诉 | 匿名发帖、查看详情、回复 |

## 5. 本次重构要点

1. **ORM 迁移**：JPA → MyBatis-Plus，10 个 Entity 重写为 `@TableName` 注解（去除 `@ManyToOne` 等关系映射，改为外键字段手动关联），10 个 Repository 改为 `BaseMapper`，Service 内全部改用 `LambdaQueryWrapper`。
2. **统一返回与异常**：新增 `Result<T>`、`ResultCode`、`BusinessException`、`GlobalExceptionHandler`，Controller 去除 try-catch，改为声明式抛业务异常。
3. **分层规范化**：Service 拆为接口 + 实现类，Controller 依赖接口；严格区分 Entity / DTO / VO。
4. **安全加固**：JWT 认证统一由 `JwtAuthFilter` 解析；资源进度接口 `userId` 从 token 获取，修复越权；`/uploads/**` 静态资源放行。
5. **敏感配置拆分**：数据库密码、JWT 密钥、AI Key 等移至 `application-local.yaml`（已加入 `.gitignore`），配置占位符带默认值。
6. **数据库切换**：H2 → MySQL，提供 `schema.sql` / `data.sql` 自动建表与初始化，`DataInitializer` 启动时幂等创建测试数据。
7. **功能补齐**：
   - 聊天亲子关系校验（新增错误码 `NOT_FAMILY`）；
   - 相册改为 `MultipartFile` 真实上传（新增 `FileStorageService` 与 `/uploads/**` 静态映射）；
   - 敏感词库从 `sensitive-words.txt` 文件加载，替代硬编码。

## 6. 测试数据与启动说明

- 测试账号：`parent1 / 123456`，含儿童「小明」「小红」（由 `DataInitializer` 启动时创建）。
- 启动前：本机需有 MySQL（默认 `root` 空密码，可在 `application-local.yaml` 覆盖），`createDatabaseIfNotExist` 自动建库。
- 启动命令：`./mvnw spring-boot:run`（或 IDEA 直接运行 `LbChildrenApplication`）。
- 上传文件默认保存到 `./uploads/`，访问前缀 `http://localhost:8080/uploads/...`（可配置 `app.upload-dir` / `app.upload-base-url`）。

## 7. 待办/注意事项

- 首次 `mvnw` 编译需联网下载依赖；当前编译验证受本机 PowerShell 执行策略限制未能在终端执行，代码已通过静态检查。
- AI 接口需在 `application-local.yaml` 配置真实的 `ai.api.key`，否则返回"AI 服务暂时不可用"。
